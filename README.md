# Шаблонизация правил алертов в Helm и их обработка через vmalert и Impulse для отправки в Telegram

## Введение

В современной микросервисной архитектуре эффективная система алертинга — критически важный компонент observability-стека. Однако поддержка большого количества правил алертов, их версионирование и доставка уведомлений в различные каналы связи представляют существенные вызовы для команд DevOps и SRE.

Данная статья описывает практический подход к построению масштабируемой системы алертинга на базе Helm-шаблонов, vmalert, Alertmanager и Impulse, обеспечивающей централизованное управление правилами и доставку уведомлений в Telegram. Решение демонстрирует полный цикл от генерации метрик Golden Signal до получения алертов в мессенджер.

## Архитектура решения

Архитектура системы алертинга состоит из нескольких взаимосвязанных компонентов:

```
Go-приложение → Helm-шаблоны → Prometheus-метрики → vmalert → Alertmanager → Impulse → Telegram
```

**Компоненты:**

1. **Go-приложение** — генерирует метрики Golden Signal (Latency, Traffic, Errors, Saturation)
2. **Helm-чарты** — шаблонизируют правила алертов (`PrometheusRule`) из values-файлов
3. **vmalert** — оценивает правила и генерирует события алертов
4. **Alertmanager** — маршрутизирует и группирует алерты
5. **Impulse** — адаптер для преобразования алертов и отправки в Telegram

Разделение ответственности позволяет независимо масштабировать каждый компонент и упрощает поддержку системы.

## Генерация метрик Golden Signal

### Концепция Golden Signal

Golden Signal — это четыре ключевых метрики, необходимых для мониторинга любого сервиса:
- **Latency** — задержка обработки запросов
- **Traffic** — пропускная способность и объем запросов
- **Errors** — частота ошибок
- **Saturation** — степень загрузки ресурсов

### Реализация в Go

Приложение на Go использует библиотеку `prometheus/client_golang` для экспорта метрик:

```go
var (
    requestLatency = prometheus.NewHistogram(...)  // Latency
    requestCount = prometheus.NewCounter(...)       // Traffic
    errorCount = prometheus.NewCounter(...)         // Errors
    saturationGauge = prometheus.NewGauge(...)      // Saturation
)
```

Метрики экспортируются через стандартный endpoint `/metrics`, совместимый с Prometheus. Гистограммы используются для измерения задержки, счетчики — для трафика и ошибок, а gauge — для контроля насыщения (например, количество активных горутин).


### VM K8s Stack (метрики, Grafana)

Пример установки victoria-metrics-k8s-stack c Grafana:

```bash
helm upgrade --install vmks \
  oci://ghcr.io/victoriametrics/helm-charts/victoria-metrics-k8s-stack \
  --namespace vmks \
  --create-namespace \
  --wait \
  --version 0.67.0 \
  --timeout 15m \
  -f vmks-values.yaml
```

Содержимое vmks-values.yaml:
```
grafana:
  ingress:
    ingressClassName: nginx
    enabled: true
    hosts:
      - grafana.apatsev.org.ru
    annotations:
      nginx.ingress.kubernetes.io/ssl-redirect: "false"
defaultRules:
  groups:
    etcd:
      create: false
kube-state-metrics:
  metricLabelsAllowlist:
    - pods=[*]
vmsingle:
  enabled: false
vmcluster:
  enabled: true
  ingress:
    select:
      enabled: true
      ingressClassName: nginx
      annotations:
        nginx.ingress.kubernetes.io/ssl-redirect: "false"
      hosts:
        - vmselect.apatsev.org.ru
alertmanager:
  enabled: true
  ingress:
    enabled: true
    ingressClassName: nginx
    hosts:
      - alertmanager.apatsev.org.ru
    annotations:
      nginx.ingress.kubernetes.io/ssl-redirect: "false"
vmalert:
  enabled: true
  ingress:
    enabled: true
    ingressClassName: istio
    hosts:
      - vmalert.apatsev.org.ru
    annotations:
      nginx.ingress.kubernetes.io/ssl-redirect: "false"
    path: "/"
    pathType: Prefix
```

Можно анализировать логи через explore Grafana.
Откройте http://grafana.apatsev.org.ru/
Откройте http://vmselect.apatsev.org.ru
Откройте http://alertmanager.apatsev.org.ru
Откройте http://vmalert.apatsev.org.ru
Для получения пароля admin от Grafana необходимо:

```bash
kubectl get secret vmks-grafana -n vmks -o jsonpath='{.data.admin-password}' | base64 --decode; echo
```

```bash
# Предварительный просмотр сгенерированных манифестов
helm template ./chart

# Валидация синтаксиса
helm lint ./chart
```

### Установка через Helm

Для установки приложения в Kubernetes-кластере используйте Helm:

```bash
# Установка приложения через Helm
helm upgrade --install golden-signal-app ./chart \
  --namespace monitoring \
  --create-namespace \
  --set image.repository=ghcr.io/patsevanton/alert-templates-helm-vmalert-impulse \
  --set image.tag=1.3.0
```

# Проверка статуса развертывания
```
kubectl get pods -n monitoring -l app=golden-signal-app
```

# Проверка метрик
```
kubectl port-forward -n monitoring svc/golden-signal-app 8080:8080
curl http://localhost:8080/metrics
curl http://localhost:8080/work
```

## Шаблонизация правил алертов в Helm

### Преимущества Helm-шаблонизации

Использование Helm для управления правилами алертов предоставляет следующие преимущества:

- **Версионирование** — правила хранятся в Git вместе с кодом приложения
- **Переиспользование** — общие шаблоны для множества сервисов
- **Параметризация** — настройка через values-файлы без изменения шаблонов
- **Единообразие** — стандартизация имен метрик и лейблов

### Структура шаблонов

Шаблоны `PrometheusRule` создаются с использованием функций Sprig и вспомогательных шаблонов Helm. Параметры правил (severity, thresholds, descriptions) выносятся в values-файлы:

```yaml
# values.yaml
alerts:
  latency:
    enabled: true
    threshold: "0.5"
    severity: "warning"
    description: "High request latency detected"
  
  errors:
    enabled: true
    threshold: "0.05"
    severity: "critical"
    description: "Error rate exceeds threshold"
```

Шаблон генерирует `PrometheusRule` ресурсы, которые автоматически подхватываются vmalert через discovery механизм Kubernetes.

### Практические рекомендации

- Используйте вспомогательные шаблоны (`_helpers.tpl`) для генерации общих меток и аннотаций
- Применяйте условную логику (`if/else`) для включения/отключения групп алертов
- Валидируйте значения через функции Helm перед генерацией правил
- Группируйте алерты по доменам (например, `application`, `infrastructure`, `business`)

## Обработка алертов через vmalert и Alertmanager

### Роль vmalert

vmalert выполняет оценку правил алертов, которые определены в `PrometheusRule` ресурсах. Компонент периодически запрашивает VictoriaMetrics (или Prometheus) и вычисляет выражения правил. При срабатывании условия алерт генерируется и отправляется в Alertmanager.

Преимущества vmalert:
- Высокая производительность благодаря интеграции с VictoriaMetrics
- Поддержка нативных Kubernetes ресурсов `PrometheusRule`
- Низкая задержка оценки правил

### Конфигурация vmalert

Правила алертов монтируются в vmalert через ConfigMap или как отдельные ресурсы `PrometheusRule`. vmalert автоматически обнаруживает изменения в правилах и перезагружает их без перезапуска.

### Маршрутизация через Alertmanager

Alertmanager получает алерты от vmalert и выполняет:

- **Группировку** — объединение похожих алертов для уменьшения шума
- **Маршрутизацию** — направление алертов в различные каналы на основе меток
- **Ингибицию** — подавление менее критичных алертов при наличии более серьезных
- **Повторы** — повторная отправка неразрешенных алертов

Конфигурация маршрутов позволяет направлять критические алерты в Telegram, предупреждения — в Slack, а информационные — только в лог-систему.

## Интеграция с Impulse и отправка в Telegram

### Архитектура Impulse

Impulse выступает адаптером между Alertmanager и внешними системами уведомлений. Компонент принимает webhook-запросы от Alertmanager и преобразует их в формат, понятный целевой системе (Telegram, Slack, PagerDuty и др.).

### Преимущества использования Impulse

- **Гибкость форматирования** — настраиваемые шаблоны сообщений
- **Агрегация контекста** — добавление ссылок на Grafana, метрики, логи
- **Управление каналами** — единая точка для всех внешних интеграций
- **Ретрансляция** — возможность отправки в несколько каналов одновременно

### Установка Impulse

Для установки Impulse через Helm используйте следующие команды:

```bash
helm repo add eslupmi https://eslupmi.github.io/helm-charts/packages
helm repo update
helm install impulse eslupmi/impulse
```

### Настройка Telegram-бота

Для отправки уведомлений в Telegram:

1. Создайте бота через [@BotFather](https://t.me/BotFather)
2. Получите токен бота
3. Настройте Impulse на прием webhook от Alertmanager
4. Сконфигурируйте шаблоны сообщений с необходимыми полями:
   - Severity (уровень серьезности)
   - Текущее значение метрики
   - Пороговое значение
   - Ссылки на дашборды Grafana
   - Команда ответственных

### Форматирование уведомлений

Impulse позволяет создавать структурированные сообщения с использованием Markdown:

```
🚨 *Критический алерт*

*Метрика:* app_errors_total
*Текущее значение:* 0.08
*Порог:* 0.05
*Сервис:* golden-signal-app
*Namespace:* production

[Открыть в Grafana](https://grafana.example.com/...)
```

## Практическая реализация

### Развертывание стека

Для развертывания полного стека мониторинга и алертинга используется Terraform и Helm:

1. **Инфраструктура** (Terraform):
   - Создание Kubernetes-кластера
   - Настройка сетей и балансировщиков
   - Выделение статических IP-адресов

2. **Приложения** (Helm):
   - Установка VictoriaMetrics Operator
   - Развертывание vmalert и Alertmanager
   - Установка Impulse
   - Применение Helm-чарта с правилами алертов

### Workflow разработки правил

Рекомендуемый процесс разработки и внедрения новых правил:

1. Определение метрики и пороговых значений
2. Создание/обновление шаблона в Helm-чарте
3. Добавление параметров в values-файл
4. Тестирование шаблона локально (`helm template`)
5. Коммит изменений в Git
6. Применение через CI/CD или вручную (`helm upgrade`)
7. Валидация работы алерта через тестовые условия

### Мониторинг системы алертинга

Важно отслеживать работоспособность самой системы алертинга:

- Метрики vmalert (количество правил, частота оценки)
- Состояние Alertmanager (доставка уведомлений, ошибки)
- Статус Impulse (обработанные webhook, ошибки отправки)
- Качество правил (соотношение сработавших/решенных алертов)

## Заключение

Представленное решение демонстрирует комплексный подход к построению масштабируемой системы алертинга на базе современных open-source инструментов. Ключевые преимущества:

- **Масштабируемость** — независимое масштабирование компонентов
- **Гибкость** — легкое добавление новых правил и каналов уведомлений
- **Прозрачность** — все правила версионируются в Git
- **Надежность** — отказоустойчивая архитектура с резервированием
- **Поддерживаемость** — четкое разделение ответственности между компонентами

Использование Helm для шаблонизации правил, vmalert для оценки, Alertmanager для маршрутизации и Impulse для доставки создает надежную и легко расширяемую платформу для мониторинга и алертинга, соответствующую лучшим практикам DevOps и SRE.
